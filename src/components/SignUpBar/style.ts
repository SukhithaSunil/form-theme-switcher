import { styled } from "@mui/material/styles";

const Wrapper = styled("div")(() => ({
  width: "100%",
}));

const SubWrapper = styled("div")(() => ({
  display: "flex",
}));

export { Wrapper, SubWrapper };
